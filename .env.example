# Optional. If omitted, render-job resolves relative inputs from ./jobs.
JOBS_ROOT=./jobs

# Optional. If omitted, outputs go to ./renders.
RENDERS_ROOT=./renders

# Optional. Must stay inside ./public so Remotion can read audio via staticFile().
PUBLIC_AUDIO_ROOT=./public/audio

# Optional. If omitted, the script tries system ffmpeg/ffprobe, then npm fallback packages.
FFMPEG_PATH=/usr/bin/ffmpeg
FFPROBE_PATH=/usr/bin/ffprobe

# Optional. Recommended on Linux VPS if Chromium is not discovered by Remotion.
# Ubuntu 24.04 may install Chromium as /snap/bin/chromium.
REMOTION_BROWSER_EXECUTABLE=/usr/bin/chromium

# Async HTTP API for Coze callbacks. Keep the API behind Nginx and do not expose
# this internal listener directly to the public internet.
HANDDRAWN_API_HOST=127.0.0.1
HANDDRAWN_API_PORT=3003
HANDDRAWN_API_TOKEN=replace-with-a-long-random-token

# Public MP4 copy target and public URL prefix served by Nginx.
PUBLIC_DOWNLOAD_ROOT=/var/www/shudan-assets/handdrawn
PUBLIC_DOWNLOAD_BASE_URL=http://129.146.22.243:80/assets/handdrawn

# The current render pipeline is validated as single-concurrency.
MAX_CONCURRENT_RENDERS=1
